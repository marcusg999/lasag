<h1>All users</h1>

<%= will_paginate %>

<ul class="users">
  <% @users.each do |user| %>
  <li>
  <%= link_to user.name, "users/#{user.id}" %>
  </li>
    <%= render user %>
  <% end %>
</ul>

<%= will_paginate %>
