<h1>All users</h1>
<% provide(:title, 'All users') %>
<%= will_paginate %>

<ul class="users">
  <% @users.each do |user| %>
  <%= render user %>
  <li>
  <%= link_to user.name, "users/#{user.id}" %>
  </li>

  <% end %>
</ul>

<%= will_paginate %>
